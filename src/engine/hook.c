#include "engine.h"
#include "inputhandler.h"
#include "mlx.h"
#include "std__system.h"

// void engine__attach_hook(t_engine *engine, int event, int mask,
// 												 t_callback callback, void *data) {
// 	mlx_hook(engine->window, event, mask, callback, data);
// }

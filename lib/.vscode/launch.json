{
  "configurations": [
    {
      "name": "Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/test/test.out",
      "cwd": "${workspaceFolder}",
      "args": []
    }
  ]
}
